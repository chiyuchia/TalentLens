services:
  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    env_file:
      - ./.env.production
    environment:
      DATABASE_URL: sqlite:////data/talentlens.sqlite3
      UPLOAD_DIR: /uploads
    volumes:
      - talentlens_data:/data
      - talentlens_uploads:/uploads
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

volumes:
  talentlens_data:
  talentlens_uploads:

